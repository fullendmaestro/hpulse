ADK_DEBUG="false" # enable this for framework logs
GOOGLE_API_KEY= # get it from https://aistudio.google.com/api-keys
LLM_MODEL="gemini-2.5-flash" # LLM model to use

# Blockchain Configuration
WALLET_PRIVATE_KEY= # Private key for blockchain transactions (optional, required for write operations)
DEFAULT_NETWORK="sepolia" # Default EVM network (sepolia, polygon-amoy, optimism-sepolia, etc.)

# Server Configuration
PORT=3000 # HTTP server port
HOST="localhost" # HTTP server host
DATABASE_PATH="./data/hpulse.db" # Path to SQLite database file
CORS_ORIGIN="*" # CORS origin for API requests
